// 숫자를 더하는 것과 문자를 더하는 것은 다르다.
1 + 1 //2
"1" + "1" //"11"

// indexOf: 문자열 내에서 index(특정한 문자열의 위치) 값을 리턴한다.
"code".indexOf("c") // 0
"code".indexOf("o") // 1
"code".indexOf("d") // 2
"code".indexOf("e") // 3
