// 배열
// 변수가 하나의 데이터를 저장하기 위한 것이라면 배열은 여러 개의 데이터를 하나의 변수에 저장하기 위한 것이라 할 수 있다.
let member = ['egoing', 'k8805', 'sorialgi']

// 인덱스(index, 색인)
// 배열이라는 그릇 안에 데이터 3개가 담겨 있고, 이 각각의 값들을 원소라한다.
// 이 원소들은 각각 '식별자' 라는 고유한 번호를 가진다. 
// 첫번째('egoing')은 0, 두번째('k8805')는 1, 세번째('sorialgi')는 2라는 값을 가진다.
// 이것을 인덱스라고 부른다.

// 배열에 담긴 어떠한 값을 꺼내오고 싶은 경우?
alert(member[1])    // k8805